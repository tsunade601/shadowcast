# shadowcast
A decentralized, client-side P2P mesh network engine that mirrors and streams static web assets directly browser-to-browser using WebRTC, drastically reducing server bandwidth and hosting costs.
